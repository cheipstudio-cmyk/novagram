<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cheipgram"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Cheipgram">
            <!--
                Theme deeplink. Tapping a nova://theme?data=<base64-json>
                URL in any app (Telegram, browser, mail, Slack...) routes
                here. MainActivity handles the intent in onCreate/onNewIntent,
                decodes the payload, and applies it via AppSettings.
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="novagram" android:host="theme" />
            </intent-filter>
            <!--
                t.me / telegram.me / telegram.dog links. Tapping a t.me
                URL in any app should route into Novagram, where MainActivity
                resolves the username/joinhash via TDLib and navigates to
                the matching chat. autoVerify stays false because we
                deliberately want the user to be prompted with a chooser the
                first time — Telegram's own client and ours are both valid
                handlers.
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="t.me" />
                <data android:scheme="http" android:host="t.me" />
                <data android:scheme="https" android:host="telegram.me" />
                <data android:scheme="http" android:host="telegram.me" />
                <data android:scheme="https" android:host="telegram.dog" />
                <data android:scheme="http" android:host="telegram.dog" />
                <data android:scheme="tg" />
            </intent-filter>
        </activity>

        <!--
            Two LAUNCHER aliases for the dark / light icon variants. The
            user's theme preference drives which one is enabled at any
            given moment, via PackageManager.setComponentEnabledSetting
            (see IconAliasManager). The Dark alias is the install
            default (enabled in the manifest, matching our default
            theme); Light is disabled until the user picks Light /
            system-light. Both targetActivity .MainActivity so the
            running code is identical regardless of which icon got
            tapped.

            Note: targetActivity must NOT itself have a LAUNCHER
            intent-filter — Android would then show two icons in the
            launcher at once. Hence the main MainActivity above carries
            only deeplink filters now, and the aliases own the
            MAIN/LAUNCHER pair.
        -->
        <activity-alias
            android:name=".LauncherDark"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dark"
            android:roundIcon="@mipmap/ic_launcher_dark_round"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".LauncherLight"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_light"
            android:roundIcon="@mipmap/ic_launcher_light_round"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service
            android:name=".notifications.TdService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!--
            Inline-reply receiver. Not exported: only the system can fire
            this via the PendingIntent we attach to message notifications.
        -->
        <receiver
            android:name=".notifications.ReplyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.secondream.novagram.action.REPLY" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
